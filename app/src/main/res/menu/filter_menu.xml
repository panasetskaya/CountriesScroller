<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/toolbar_menu_filter"
        android:icon="@drawable/filter_on"
        android:title="@string/filter_title"
        app:showAsAction="always" />
    <item
        android:id="@+id/toolbar_menu_filter_off"
        android:icon="@drawable/filter_off"
        android:title="@string/unfilter_title"
        app:showAsAction="always" />

    <item
        android:id="@+id/toolbar_search"
        android:icon="@drawable/ic_search"
        android:title="@string/search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always"/>

</menu>